# C01：跨线服务端接口变更单模板

> 实际申请文件保存为 `docs/contracts/change-requests/CR-<任务>-<序号>.md`。本模板不包含任何真实密钥、个人信息或测试账号口令。

## 1. 基本信息

- 变更单编号：`CR-<任务>-<序号>`
- 发起泳道与负责人：
- 关联任务与验收编号：
- 当前消费契约版本：
- 优先级与阻塞级别：
- 创建时间与发起分支/HEAD：

## 2. 可复现场景

- 用户角色与匿名测试夹具：
- 前置项目/资格状态：
- 当前请求与已脱敏响应：
- 预期行为：
- 实际阻塞：
- 为什么现有接口无法安全满足：

## 3. 最小向后兼容建议

- 建议端点或 RPC：
- 字段与原因码：
- 权限、RLS 和最小展示要求：
- 幂等键、并发或弱网重试要求：
- 审计事件：
- 契约版本与旧客户端兼容策略：

## 4. 验证与影响

- 所需契约测试与安全负向测试：
- 所需客户端 Mock：
- 对 Web/H5、小程序、数据库迁移、文件存储和发布的影响：
- 临时处理：仅安全 Mock 或隐藏未解锁功能；不得复制服务端规则或放宽权限。

## 5. Web/后端响应

响应不得改写本申请。Web/后端在同目录新建 `CR-<任务>-<序号>-response.md`，记录接受/拒绝、实际契约版本、实现提交、测试、回滚和交接文件。
