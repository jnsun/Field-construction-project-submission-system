# 物化院有限公司安全教育平台 Web/后端快速开发规则

1. 正式 Web/后端工作区为 `E:\codex\safety-web`，主线分支为 `track/web-backend`。
2. 从当前交接状态继续，不得重新从 D00 开始。
3. 每个 D 是业务里程碑，一次只推进当前一个 D 编号。一个 D 可以拆成多个内部开发步骤，但这些步骤只是执行切片，不是独立里程碑：默认连续推进，不要求逐步 CANDIDATE/PASS、逐步 R01/R03、逐步 commit/push 或逐步交接；建议每个新 D 使用新的 Codex 对话，但仍绑定 `E:\codex\safety-web` 和 `track/web-backend`。
4. 开发优先尽快完成可上线试点的核心业务，不做无关重构、过度设计、非必要文档和提前优化。默认采用“一个 D 连续开发包”：在没有 P0/P1、明确阻塞或必须人工介入的情况下，不因内部小步骤完成而停下来等待总控确认。
5. 快速开发主流程固定为：**连续开发当前 D 的内部步骤 → FAST / 必要 TARGETED → 当前 D 全部功能完成 → 一次 FINAL → CANDIDATE → 一次 R01 → 仅在存在 P0/P1/阻塞时执行正式 R02 → 必要 TARGETED/FINAL → R03（交接 + 显式 commit）→ PASS → 安全 fast-forward push**。不得把内部步骤重复套用这套正式流程。
6. 测试采用分层执行：
   - **FAST**：每次代码修改后，只运行直接相关的短测试。
   - **TARGETED**：内部步骤或缺陷修复完成后，只运行受影响的数据库、RLS、API、页面或模块测试；不要求每个内部步骤都跑同一套 TARGETED。
   - **FINAL**：当前 D 的所有功能完成并准备收口时，原则上只运行一次该 D 的完整验收。
   - **GATE**：只有 G0/G1/G2/G3/G4/G5/G6、D27、D37 等阶段门或发布门，才运行跨系统全量测试。
7. 不得把“完成后运行现有测试”解释为每次小修改都运行全仓 lint、全部单元、全部 E2E、全迁移恢复和全部安全测试。
8. focused 只读复核不是内部步骤的默认门禁。只有总控/用户明确要求，或已有具体失败/证据表明可能存在 P0/P1 且现有 TARGETED 不能充分判断时才使用；不得仅因“权限/RLS/Storage 属于高风险”就自动追加复核。内部开发中发现 P0/P1 时执行“最小复现 → focused 修复 → 对应 TARGETED”，这不记作正式 R02。修复通过且没有新的具体 P0/P1 证据后立即继续下一内部步骤，禁止形成“复核→修复→再复核→再复核”的无限循环。
9. 同一个失败最多直接重跑一次。第二次出现同类失败后，必须停止整套重跑，改为最小复现和定向修复。
10. 完整 FINAL 失败后，先单独修复失败项并通过 TARGETED；所有阻塞项修完后才允许再运行一次必要 FINAL，不得每修一个小问题就重新跑整套 FINAL。
11. R01/R02/R03 只对完整 D 执行：
   - 当前 D 全部开发完成并达到 CANDIDATE 后执行一次 R01；
   - 正式 R02 只在 R01 发现 P0/P1/阻塞项时执行；
   - R02 只修 P0/P1/阻塞，并先跑缺陷相关最小测试/TARGETED；
   - 阻塞项全部关闭后仅做必要的 FINAL；
   - R01 PASS 后执行一次 R03；
   - Dxx-1、Dxx-A/B/C 等内部步骤不得单独要求正式 R01-R03。
12. 每个 D 必须单独验收、单独提交、单独交接，但不强制每个 D 新建 Git 分支，默认继续使用 `track/web-backend`。内部步骤默认不 commit、不 push；只有确有独立生产/安全修复需要隔离收口时，才由总控明确批准例外提交。
13. 新 D 第一轮只读取当前交接、当前 D 任务、`git status`/当前 diff、受影响模块、必要契约和最近测试结果，不得无条件重新盘点整个仓库。
14. 同一 D 后续轮次只读取当前 diff、最近失败日志和直接相关代码；信息不足时再扩大读取范围。
15. 当前 D 中发现的问题按以下规则处理：
   - 当前修改引入的问题：当前 D 修；
   - 当前验收明确要求的问题：当前 D 修；
   - P0/P1 或真正阻塞当前任务的问题：立即最小修复，不带入下一阶段；
   - P2/P3 或与当前 D 无关的问题：记录到追踪/交接，默认后移，不得因此反复暂停当前主线；
   - 已登记 P2/P3 只有在出现新证据升级为 P0/P1 时才重新阻塞；
   - 不得把所有历史技术债吸收到当前 D。
16. Web/后端是数据库、迁移、RLS、认证、API、权限和业务状态机的唯一来源。
17. 不得修改 `miniprogram/**`。小程序接口缺口走 C01。
18. 不访问生产环境，不输出、记录或提交任何真实密码、密钥、令牌或敏感测试数据。
19. 不得通过关闭 RLS、放宽权限、跳过关键测试、吞掉错误或硬编码结果来获得 PASS。
20. 数据库迁移继续连续编号，不回写已经执行过的历史迁移。
21. 每轮结束只汇报：完成了什么；测试结果和耗时；当前 D 状态；下一步唯一动作。
22. 不使用 `git add .`。
23. `docs/plans/` 默认不纳入 D 业务提交；仅在用户明确要求维护并提交项目规则时例外。

## GitHub 阶段成果同步规则

1. Web/后端本地开发分支固定为 `track/web-backend`，GitHub 阶段成果分支为 `origin/training-module`。
2. D 内的 FAST、TARGETED、R02 和其他小步骤不得 push。
3. 只有当前 D 已完成必要验收、R01 PASS，并在 R03 中成功 commit、正式标记 PASS 后，才允许执行一次 GitHub 同步。
4. push 前必须执行 `git fetch origin training-module`，并确认 `origin/training-module` 是当前 `HEAD` 的祖先，或存在其他明确安全的 fast-forward 条件。
5. 禁止 `--force`、`--force-with-lease`、自动 merge、自动 rebase 和覆盖未知远端提交；发现分叉立即停止并汇报。
6. 安全条件满足后执行 `git push origin HEAD:training-module`。
7. push 后必须核对本地 `HEAD`、远端 `training-module` HEAD 和未推送 commit 数量。
8. 本规则只适用于 Web/后端主线；小程序线不得擅自 push 到 `training-module`。

## D11 已确认业务规则

1. 三级安全教育只适用于新建立正式劳动/用工关系、具有服务端权威 `employment_relation_id` 且尚未完成基础三级教育的内部员工；不得用 `created_at`、加入项目时间或前端布尔值判断。离职后重新建立正式用工关系按重新入职处理。
2. 老员工有可信完整历史时记为 `legacy_verified`；无可信历史时先进入 `legacy_evidence_review`，必要时进入 `legacy_supplement`，真实补训完成后记为 `legacy_supplement_completed`，不得伪造原入职培训时间。
3. 一段正式用工关系内三级教育只完成一次。完成或补齐后，换项目、跨经营实体内部调动、跨年度或项目关闭均不重做。
4. 第三级支持 `basic_project`（无需实际项目 ID）和 `actual_project`（必须绑定合法正式项目）；任一种完成均满足基础第三级。
5. 外协、`temporary_individual` 和访客不适用本公司员工三级教育。外协和临时个人走项目准入路径；临时个人不得伪造单位、合同或统一社会信用代码，后续须绑定责任经营实体、当前项目、接收/审核责任人和可审计进场依据；访客走安全告知路径。
6. 后续持续培训按法规/制度适用、变化内容和事件触发，由 D17 配置。默认只学习新增或变化内容，并记录来源、版本、生效时间、变化摘要、适用人群和必学内容；除正式规则明确要求外，不得按年度重做整套三级教育。
7. 爆破、电工、焊工专项只在“项目 + 人员实际从事该专项作业”时触发，持证本身不自动触发。钻探不存在个人“钻探证”；`site_projects.includes_drilling=true` 时，该项目全部 active personnel 纳入钻探专项安全培训。
