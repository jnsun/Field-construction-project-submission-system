# 本地项目配置/数据（含 MCP、项目级 skills、自动化等）—— 不进版本库
.workbuddy/

# 第三方 SDK 必须随站点一起部署（本项目没有构建步骤，浏览器直接引用），
# 因此不再忽略 vendor/。历史上这里被忽略过，导致 xlsx / pdf 两个库在
# GitHub Pages 与服务器上一直是 404，Excel 导入与 PDF 阅读功能不可用。
# 如确需排除某个大文件，请在下方显式写文件名，不要整体忽略目录。

# 通用忽略
node_modules/
.env
.env.*
!.env.example
backups/
js/config.local.js
test-results/
*.log
.DS_Store
Thumbs.db
.idea/
.vscode/
